{% extends 'base.html.twig' %}
{% block title %}
    {% if edit is defined %}
        Editar País
    {% else %}    
        Crear País
    {% endif %}
{% endblock %}
{% block body %}

<div class="example-wrapper">
    <h2>{% if edit is defined %}
        Editar País
    {% else %}    
        Crear País
        {% endif %}</h2>
   {{ form_start(form) }}
   {{ form_widget(form) }}
   {{ form_end(form) }}
</div>
{% endblock %}{# empty Twig template #}{# empty Twig template #}
